{
  "name": "midnight-voice",
  "version": "1.0.0",
  "description": "MIDNIGHT — a voice game-master for in-person social deduction (Mafia).",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "test": "node scripts/test-engine.js && node scripts/intents-test.js && node scripts/smoke-game.js && node scripts/run-sockets.js",
    "prerender": "node scripts/prerender-script.js"
  },
  "dependencies": {
    "ws": "^8.21.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
